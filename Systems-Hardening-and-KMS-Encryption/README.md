## 🗂️ Logs & Artifacts
- `logs/ssm-patch-scan-linux.json` — Pre‑install SCAN results for Linux (what was missing).
- `logs/ssm-patch-install-linux.json` — Post‑install results (what was installed).
- `logs/ssm-compliance-summary.json` — At‑a‑glance compliance across Linux & Windows.
- `logs/windows-custom-baseline.json` — The custom Windows Patch Baseline definition used.
- `logs/kms-encrypt-result.json` — Example output from a KMS Encrypt call.
- `logs/kms-decrypt-result.json` — Example output from a KMS Decrypt call.
- `logs/patch-operation-history.txt` — Human‑readable timeline of patching actions.
